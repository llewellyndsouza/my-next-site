import Head from 'next/head';
import styles from '../../styles/Layout.module.css';
import Footer from './footer';
import Header from './header';
import { motion } from 'framer-motion';
import { useContext, useState } from 'react';
import { ThemeContext } from '../context/theme';

export default function Layout({ children, title }) {
  const { isThemeDark } = useContext(ThemeContext);

  return (
    <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} className={`${styles.layoutContainer} ${isThemeDark ? styles.dark : null}`}>
      <Head>
        <title>Llewellyn - {title}</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
        {/* <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin /> */}
        <link href="https://fonts.googleapis.com/css2?family=Inconsolata:wght@200;300;500&display=swap" rel="stylesheet"></link>
      </Head>
      <Header />
      {/* <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{delay: 0.5}} className={styles.contentContainer}>{children}</motion.div> */}
      {children}
      <Footer />
    </motion.div>
  );
}
